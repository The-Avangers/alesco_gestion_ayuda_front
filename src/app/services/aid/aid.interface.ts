export interface Aid {
    id: number;
    name: string;
    measure: string;
    type: string;
    unit: BigInteger;
}

export interface PostAid {
    name: string;
    measure: string;
    type: string;
    unit: number;
}
